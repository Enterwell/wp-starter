/**
 * External dependencies
 */
import styled from '@emotion/styled';

/**
 * WordPress dependencies
 */
import { NavigableMenu } from '@wordpress/components';

export const StyledMenu = styled( NavigableMenu )`
	display: flex;
	flex-direction: column;
	gap: 0.5rem;
`;
