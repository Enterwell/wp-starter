import React from 'react';
import { hot } from 'react-hot-loader/root';

// Home view
const Home = () => <h1>React radi!!!</h1>;

// Export component as hot-exported (enableing hot module reload)
export default hot(Home);
